# 🚀 CRM Reflex - Деплой на Render.com

## 📦 Структура проекта:

```
crm-reflex/
├── crm_reflex.py      # Главный файл
├── rxconfig.py        # Конфигурация Reflex
├── requirements.txt   # Зависимости
└── README.md         # Эта инструкция
```

## 📤 Шаги деплоя:

### 1. Создай репозиторий GitHub
- Название: `crm-reflex`
- Загрузи все 4 файла

### 2. Зайди на Render.com
- https://render.com
- Войди/зарегистрируйся

### 3. Создай Web Service
1. New+ → Web Service
2. Connect GitHub → выбери `crm-reflex`
3. Настрой команды (ВАЖНО!):

**Build Command:**
```bash
pip install --upgrade pip && pip install -r requirements.txt && reflex init
```

**Start Command:**
```bash
reflex run --env prod --backend-port 8000
```

**Environment:**
- Region: Frankfurt (или любой)
- Plan: Free

4. Create Web Service
5. Жди 10-15 минут

## 🔑 Логины:

После деплоя открой URL и войди:
- **Head**: `admin` / `admin123`
- **Buyer**: `buyer1` / `buyer123`

## ⚠️ Проблемы?

### "Module not found"
Убедись что файл называется точно `crm_reflex.py` (с подчёркиванием)

### "No module named reflex"
Проверь Build Command - должен быть `reflex init` в конце

### Долго грузится
Free план Render засыпает после 15 минут. Первая загрузка ~30 сек.

## 💾 База данных

Сейчас используется CSV файл (не сохраняется между деплоями).

Для production:
1. Добавь PostgreSQL на Render (бесплатно)
2. Измени код для работы с PostgreSQL

## 🔄 Обновления

Просто делай `git push` - Render автоматически задеплоит!

---

**Готово!** 🎉
