# 🚀 Деплой CRM на Render.com

## 📋 Файлы для загрузки в GitHub:

```
crm-reflex/
├── crm_reflex.py         # Главный файл приложения
├── rxconfig.py           # Конфигурация Reflex
├── requirements.txt      # Зависимости
├── render.yaml          # Конфигурация Render
├── Dockerfile           # Docker конфиг (опционально)
└── README.md            # Этот файл
```

## 🎯 Шаги деплоя:

### 1. Создай репозиторий на GitHub
- Создай новый репозиторий: `crm-reflex`
- Загрузи все файлы из этой папки

### 2. Зайди на Render.com
- Открой: https://render.com
- Зарегистрируйся / войди

### 3. Создай Web Service
1. Нажми **"New +"** → **"Web Service"**
2. Выбери **"Build and deploy from a Git repository"**
3. Подключи GitHub аккаунт
4. Выбери репозиторий `crm-reflex`
5. Нажми **"Connect"**

### 4. Настройки деплоя
Render автоматически обнаружит `render.yaml` и применит настройки:

```
Name: crm-reflex
Region: Frankfurt
Branch: main
Build Command: pip install -r requirements.txt && reflex init
Start Command: reflex run --env prod --loglevel info
```

Если нужно изменить:
- **Plan**: Free (бесплатный)
- **Environment Variables**: можно добавить API ключи

### 5. Деплой!
1. Нажми **"Create Web Service"**
2. Подожди 5-10 минут
3. Получишь URL: `https://crm-reflex.onrender.com`

## ✅ После деплоя:

Открой URL и войди:
- **Head**: `admin` / `admin123`
- **Buyer**: `buyer1` / `buyer123`

## 🔄 Обновления:

Просто сделай `git push` - Render автоматически задеплоит новую версию!

## ⚠️ Важно:

**Free план Render:**
- Засыпает после 15 минут неактивности
- Первая загрузка после сна займёт ~30 сек
- Для production используй платный план ($7/мес)

## 🆘 Проблемы?

### App не запускается:
1. Проверь логи в Render Dashboard
2. Убедись что все файлы загружены в GitHub
3. Проверь `requirements.txt`

### Ошибка импорта:
Добавь недостающую библиотеку в `requirements.txt`

### База данных не сохраняется:
Free план Render очищает файловую систему при каждом деплое.
Для постоянного хранения используй PostgreSQL (также есть на Render).

## 💡 Дополнительно:

### Добавить PostgreSQL:
1. В Render создай **"New PostgreSQL"**
2. Подключи к Web Service
3. Измени код для работы с PostgreSQL вместо CSV

### Добавить свой домен:
В настройках Web Service → Custom Domain

---

## 📞 Поддержка:

Если что-то не работает - покажи логи из Render Dashboard!
